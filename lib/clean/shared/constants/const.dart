const responseCodeSuccess = "00";
const responseCodeFailure = "01";

/// Dùng để showDialog khi bên BE trả về code = '06'
const responseCodeShowDialog = "06";

const connectTimeout = Duration(minutes: 1);
const receiveTimeout = Duration(minutes: 1);
const sendTimeout = Duration(minutes: 1);
const signDocumentTimeOut = Duration(minutes: 3);

