name: v_bhxh
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.12+37

environment:
  sdk: ^3.5.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependency_overrides:
  dio: ^5.4.2+1

dependencies:
  sds_share_pdf:
    git:
      url: https://github.com/pvc97/sds_share_pdf.git
  asn1lib: ^1.5.9
  bot_toast: ^4.1.3
  collection: ^1.19.0
  cupertino_icons: ^1.0.8
  decimal: ^3.2.2
  dio: ^5.4.2+1
  # Nhánh fix/compressedBytes_failed sẽ luôn trả về ảnh dù nó có nhỏ hơn kích thước tối đa hay không,
  # thay vì trả về null nếu không nén được ảnh nhỏ hơn kích thước tối đa như nhánh main
  image_compress:
    git:
      url: https://github.com/HVLoc/image_compress.git
      ref: fix/compressedBytes_failed
  dio_log_sds:
    git:
      url: https://github.com/HVLoc/dio_log_sds.git
      ref: flutter/3.22.0
  dotted_border: ^2.1.0
  dropdown_button2: ^2.3.9
  easy_debounce: ^2.0.3
  equatable: ^2.0.7
  file_picker: ^10.1.5
  firebase_core: ^3.13.0
  flutter:
    sdk: flutter
  flutter_exif_rotation: ^0.5.2
  flutter_form_registry: ^0.7.0
  flutter_keyboard_visibility: ^6.0.0
  flutter_localizations:
    sdk: flutter
  flutter_rounded_date_picker: ^3.0.4
  flutter_secure_storage: ^9.2.4
  flutter_slidable: ^4.0.0
  flutter_svg: ^2.0.17
  get: ^4.7.2
  google_fonts: ^6.2.1
  hive: ^2.2.3
  html_unescape: ^2.0.0
  http_parser: ^4.0.2
  image_cropper: ^9.0.0
  image_picker: ^1.1.2
  intl: ^0.19.0
  loading_overlay_pro: ^2.1.1
  logger: ^2.5.0
  month_picker_dialog: 6.0.3
  package_info_plus: ^8.3.0
  path: ^1.9.0
  path_provider: ^2.1.5
  percent_indicator: ^4.2.5
  permission_handler: ^11.4.0
  pin_code_fields: ^8.0.1
  pull_to_refresh: ^2.0.0
  syncfusion_flutter_core: 29.1.38
  syncfusion_flutter_pdfviewer: 29.1.38
  tiengviet: ^1.0.0
  timelines_plus: ^1.0.6
  url_launcher: ^6.3.1
  uuid: ^4.5.1
  share_plus: ^11.0.0
  flutter_holo_date_picker: ^2.0.0
  device_info_plus: ^11.3.0
  flutter_file_dialog: ^3.0.2
  envied: ^1.1.1
  event_bus: ^2.0.1
  multi_select_flutter:
    git:
      url: https://github.com/hiep12312391283/multi_select_flutter.git
  firebase_remote_config: ^5.5.0
  upgrader: ^11.4.0
  firebase_crashlytics: ^4.3.10
  firebase_analytics: ^11.6.0

dev_dependencies:
  flutter_test:
      sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.11
  envied_generator: ^1.1.1
  mocktail: ^1.0.4
  

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:

  # assets start

  # GENERATED CODE - DO NOT MODIFY MANUALLY
  # **************************************************************************
  # Auto generated by SoftDreams Developer
  # **************************************************************************

    - assets/fonts/
    - assets/icons/
    - assets/icons/home/
    - assets/images/
    - assets/locales/

  # assets end
  fonts:
    - family: NunitoSans
      fonts:
        - asset: assets/fonts/NunitoSans-Regular.ttf
          weight: 400
        - asset: assets/fonts/NunitoSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/NunitoSans-Bold.ttf
          weight: 700
        - asset: assets/fonts/NunitoSans-ExtraBold.ttf
          weight: 800
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Bold.ttf
          weight: 700
        - asset: assets/fonts/Roboto-Medium.ttf
          weight: 500
        - asset: assets/fonts/Roboto-Regular.ttf
          weight: 400